server:
  port: 8989
  servlet:
    context-path: /ms



mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ysy.homework.kill

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/stockdb
    username: root
    password: 123456

  redis:
    database: 0
    host: localhost
    port: 6379

md5:
  salt: '!Q*jS#'

logging:
  level:
    com:
      ysy:
        homework:
          kill:
            mapper: debug
    root: info
